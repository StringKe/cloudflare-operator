---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: cloudflarecredentials.networking.cloudflare-operator.io
spec:
  group: networking.cloudflare-operator.io
  names:
    kind: CloudflareCredentials
    listKind: CloudflareCredentialsList
    plural: cloudflarecredentials
    shortNames:
    - cfcreds
    singular: cloudflarecredentials
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.accountId
      name: Account ID
      type: string
    - jsonPath: .spec.authType
      name: Auth Type
      type: string
    - jsonPath: .spec.isDefault
      name: Default
      type: boolean
    - jsonPath: .status.validated
      name: Validated
      type: boolean
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: CloudflareCredentials is the Schema for global Cloudflare API
          credentials
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareCredentialsSpec defines the desired state of CloudflareCredentials
            properties:
              accountId:
                description: AccountID is the Cloudflare Account ID
                type: string
              accountName:
                description: AccountName is an optional human-readable account name
                  (for reference only)
                type: string
              authType:
                default: apiToken
                description: AuthType specifies the authentication method
                enum:
                - apiToken
                - globalAPIKey
                type: string
              defaultDomain:
                description: DefaultDomain is the default domain for resources using
                  these credentials
                type: string
              isDefault:
                default: false
                description: |-
                  IsDefault marks this as the default credentials for resources that don't specify credentials
                  Only one CloudflareCredentials can be marked as default
                type: boolean
              secretRef:
                description: SecretRef references the secret containing the API credentials
                properties:
                  apiKeyKey:
                    default: CLOUDFLARE_API_KEY
                    description: Key in the secret for Global API Key (used when authType
                      is globalAPIKey)
                    type: string
                  apiTokenKey:
                    default: CLOUDFLARE_API_TOKEN
                    description: Key in the secret for API Token (used when authType
                      is apiToken)
                    type: string
                  emailKey:
                    default: CLOUDFLARE_EMAIL
                    description: Key in the secret for Email (used when authType is
                      globalAPIKey)
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                  namespace:
                    default: cloudflare-operator-system
                    description: Namespace of the secret. Defaults to "cloudflare-operator-system"
                    type: string
                required:
                - name
                type: object
            required:
            - accountId
            - authType
            - secretRef
            type: object
          status:
            description: CloudflareCredentialsStatus defines the observed state of
              CloudflareCredentials
            properties:
              accountName:
                description: AccountName is the account name retrieved from Cloudflare
                  API
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastValidatedTime:
                description: LastValidatedTime is the last time credentials were validated
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              state:
                description: State represents the current state of the credentials
                type: string
              validated:
                description: Validated indicates whether the credentials have been
                  validated
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
