---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: r2bucketdomains.networking.cloudflare-operator.io
spec:
  group: networking.cloudflare-operator.io
  names:
    kind: R2BucketDomain
    listKind: R2BucketDomainList
    plural: r2bucketdomains
    shortNames:
    - cfr2d
    - r2domain
    singular: r2bucketdomain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bucketName
      name: Bucket
      type: string
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.publicAccessEnabled
      name: Public
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          R2BucketDomain is the Schema for the r2bucketdomains API
          It configures a custom domain for an R2 storage bucket
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: R2BucketDomainSpec defines the desired state of R2BucketDomain
            properties:
              bucketName:
                description: BucketName is the name of the R2 bucket to attach the
                  domain to
                minLength: 3
                type: string
              credentialsRef:
                description: |-
                  CredentialsRef references a CloudflareCredentials resource
                  If not specified, the default CloudflareCredentials will be used
                properties:
                  name:
                    description: Name of the CloudflareCredentials resource
                    type: string
                required:
                - name
                type: object
              domain:
                description: |-
                  Domain is the custom domain name to attach to the bucket
                  The domain must belong to a zone in the same Cloudflare account
                pattern: ^([a-zA-Z0-9]([a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$
                type: string
              enablePublicAccess:
                default: true
                description: |-
                  EnablePublicAccess enables public access to the bucket via this domain
                  When true, the bucket contents can be accessed without authentication
                type: boolean
              minTls:
                default: "1.2"
                description: MinTLS sets the minimum TLS version for the custom domain
                enum:
                - "1.0"
                - "1.1"
                - "1.2"
                - "1.3"
                type: string
              zoneId:
                description: |-
                  ZoneID is the Cloudflare zone ID for the domain
                  If not specified, it will be looked up automatically
                type: string
            required:
            - bucketName
            - domain
            type: object
          status:
            description: R2BucketDomainStatus defines the observed state of R2BucketDomain
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              domainId:
                description: DomainID is the Cloudflare domain configuration ID
                type: string
              enabled:
                description: Enabled indicates if the domain is enabled
                type: boolean
              message:
                description: Message provides additional information about the current
                  state
                type: string
              minTls:
                description: MinTLS is the configured minimum TLS version
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              publicAccessEnabled:
                description: PublicAccessEnabled indicates if public access is enabled
                type: boolean
              state:
                description: State represents the current state of the domain
                enum:
                - Pending
                - Initializing
                - Active
                - Error
                type: string
              url:
                description: URL is the full URL to access the bucket via this domain
                type: string
              zoneId:
                description: ZoneID is the resolved zone ID for the domain
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
