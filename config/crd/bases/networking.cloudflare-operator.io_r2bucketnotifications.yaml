---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: r2bucketnotifications.networking.cloudflare-operator.io
spec:
  group: networking.cloudflare-operator.io
  names:
    kind: R2BucketNotification
    listKind: R2BucketNotificationList
    plural: r2bucketnotifications
    shortNames:
    - cfr2n
    - r2notify
    singular: r2bucketnotification
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bucketName
      name: Bucket
      type: string
    - jsonPath: .spec.queueName
      name: Queue
      type: string
    - jsonPath: .status.ruleCount
      name: Rules
      type: integer
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          R2BucketNotification configures event notifications for an R2 bucket.
          Events are sent to a Cloudflare Queue when objects are created or deleted.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: R2BucketNotificationSpec defines the desired state of R2BucketNotification
            properties:
              bucketName:
                description: BucketName is the name of the R2 bucket to configure
                  notifications for
                type: string
              credentialsRef:
                description: |-
                  CredentialsRef references a CloudflareCredentials resource
                  If not specified, the default CloudflareCredentials will be used
                properties:
                  name:
                    description: Name of the CloudflareCredentials resource
                    type: string
                required:
                - name
                type: object
              queueName:
                description: QueueName is the name of the Cloudflare Queue to send
                  notifications to
                type: string
              rules:
                description: Rules defines the notification rules
                items:
                  description: R2NotificationRule defines a notification rule
                  properties:
                    description:
                      description: Description is a human-readable description of
                        this rule
                      type: string
                    eventTypes:
                      description: EventTypes is the list of event types to notify
                        on
                      items:
                        description: R2EventType represents the type of R2 event to
                          notify on
                        enum:
                        - object-create
                        - object-delete
                        type: string
                      minItems: 1
                      type: array
                    prefix:
                      description: Prefix filters events to objects with keys starting
                        with this prefix
                      type: string
                    suffix:
                      description: Suffix filters events to objects with keys ending
                        with this suffix
                      type: string
                  required:
                  - eventTypes
                  type: object
                minItems: 1
                type: array
            required:
            - bucketName
            - queueName
            - rules
            type: object
          status:
            description: R2BucketNotificationStatus defines the observed state of
              R2BucketNotification
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              queueId:
                description: QueueID is the Cloudflare Queue ID
                type: string
              ruleCount:
                description: RuleCount is the number of notification rules configured
                type: integer
              state:
                description: State represents the current state of the notification
                enum:
                - Pending
                - Active
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
