# Cloudflare API Credentials
# Cloudflare API 凭证
#
# IMPORTANT: Replace with your actual API token
# 重要：替换为你的实际 API token

apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-credentials
  namespace: web-app
type: Opaque
stringData:
  CLOUDFLARE_API_TOKEN: "<your-api-token>"
